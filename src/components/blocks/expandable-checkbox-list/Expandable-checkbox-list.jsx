import Dropdown from 'blocks/dropdown/Dropdown';
import defaultProps from './props';

export default class ExpandableCheckboxList extends Dropdown {
  constructor(props = defaultProps) {
    super(props);
  }
}
